---
"api": minor
"frontend": minor
---

Let players pair RFID tags from Settings: generate a Pairing Code, then scan the tag and enter the code on a Randomizer Box, which submits it via `POST /api/v3/hardware/pairing`. Players can also list and unlink their tags.
