info:
  name: Search
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_host}}/api/v1/users/search?query=b"
  params:
    - name: query
      value: b
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
