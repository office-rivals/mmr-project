info:
  name: Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_host}}/api/v1/users"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
