info:
  name: V3PersonalAccessTokens_RevokeToken
  type: http
  seq: 3
  tags:
    - V3PersonalAccessTokens

http:
  method: DELETE
  url: "{{baseUrl}}/api/v3/me/tokens/:tokenId"
  params:
    - name: tokenId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v3/me/tokens/:tokenId"
      method: DELETE
      params:
        - name: tokenId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
