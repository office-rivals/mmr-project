info:
  name: Invite_JoinOrganization
  type: http
  seq: 2
  tags:
    - Invite

http:
  method: POST
  url: "{{baseUrl}}/api/v3/invites/:code/join"
  params:
    - name: code
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v3/invites/:code/join"
      method: POST
      params:
        - name: code
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: |-
          {
            "organizationId": "",
            "organizationName": "",
            "organizationSlug": "",
            "membershipId": ""
          }
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v3/invites/:code/join"
      method: POST
      params:
        - name: code
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "organizationId": "",
            "organizationName": "",
            "organizationSlug": "",
            "membershipId": ""
          }
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v3/invites/:code/join"
      method: POST
      params:
        - name: code
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: |-
          {
            "organizationId": "",
            "organizationName": "",
            "organizationSlug": "",
            "membershipId": ""
          }
