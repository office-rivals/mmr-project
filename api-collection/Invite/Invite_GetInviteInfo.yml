info:
  name: Invite_GetInviteInfo
  type: http
  seq: 1
  tags:
    - Invite

http:
  method: GET
  url: "{{baseUrl}}/api/v3/invites/:code"
  params:
    - name: code
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v3/invites/:code"
      method: GET
      params:
        - name: code
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/plain
      body:
        type: text
        data: |-
          {
            "code": "",
            "organizationName": "",
            "organizationSlug": "",
            "isValid": false
          }
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v3/invites/:code"
      method: GET
      params:
        - name: code
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "code": "",
            "organizationName": "",
            "organizationSlug": "",
            "isValid": false
          }
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v3/invites/:code"
      method: GET
      params:
        - name: code
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/json
      body:
        type: json
        data: |-
          {
            "code": "",
            "organizationName": "",
            "organizationSlug": "",
            "isValid": false
          }
