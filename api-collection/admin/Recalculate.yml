info:
  name: Recalculate
  type: http
  seq: 1

http:
  method: POST
  url: "{{api_host}}/api/v1/admin/recalculate"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
