info:
  name: Matches
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_host}}/api/v2/mmr/matches"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
