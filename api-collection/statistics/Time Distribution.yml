info:
  name: Time Distribution
  type: http
  seq: 2

http:
  method: GET
  url: "{{api_host}}/api/v1/stats/time-distribution"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
